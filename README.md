# B's Bistro · Comfort Dining in Underberg

Static website for **B's Bistro**, a sophisticated country restaurant in Underberg, South Africa.

## Development

Open `index.html` directly in your browser, or serve the folder with any simple HTTP server:

```bash
cd "/Users/jessica-leetingley/Documents/b-bistro"
python3 -m http.server 8000
```

Then visit `http://localhost:8000` in your browser.

## Deploying

The site is a single static page and can be deployed to any static host (GitHub Pages, Netlify, Vercel, etc.) by serving the contents of this folder.


